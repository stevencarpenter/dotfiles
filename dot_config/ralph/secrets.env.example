# Ralph + OpenCode Global Configuration
# ======================================
# Chezmoi-managed configuration for Ralph/OpenCode integration.
# This file provides global defaults that can be overridden per-repo.
#
# Location: ~/.config/ralph/secrets.env (decrypted from chezmoi)
# Format: KEY=VALUE (one per line, no export prefix)
# Permissions: Must be 0600 (enforced by Ralph)
#
# Source from encrypted chezmoi file:
#   chezmoi add --encrypt ~/.config/ralph/secrets.env
#
# To edit:
#   chezmoi edit ~/.config/ralph/secrets.env
#   chezmoi apply

# LM Studio (local models) - typically doesn't need auth
LMSTUDIO_API_KEY=sk-lmstudio

# OpenRouter API token (for cloud models)
# Get from: https://openrouter.ai/keys
# OPENROUTER_TOKEN=sk-or-v1-...

# OpenAI API key (if using OpenAI directly)
# OPENAI_API_KEY=sk-...

# Anthropic API key (if using Claude API directly)
# ANTHROPIC_API_KEY=sk-ant-...

# GitHub token for repo operations (usually sourced from gh CLI)
# GITHUB_TOKEN=ghp_...
