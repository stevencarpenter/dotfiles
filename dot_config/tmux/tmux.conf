# Remap prefix from 'C-b' to 'C-Space'
unbind C-b
set -g prefix C-Space

# [ PREFIX + r ] Reload tmux config
unbind r
bind r source-file $XDG_CONFIG_HOME/tmux/tmux.conf \; display "Reload Configuration"

# Mouse support
set -g mouse on

# Put the tmux status bar on the top of the window
# set-option -g status-position top

# Move between panes vim style
bind-key h select-pane -L
bind-key j select-pane -D
bind-key k select-pane -U
bind-key l select-pane -R

# Split panes using | and -
bind | split-window -h
bind - split-window -v
unbind '"'
unbind %

# Easier and faster switching between next/prev window
bind C-p previous-window
bind C-n next-window

# TMUX Sensible settings but my tweaks
# Address vim mode switching delay (http://superuser.com/a/252717/65504)
set -s escape-time 0

# Increase scrollback buffer size from 2000 to 1000000 lines
set -g history-limit 1000000

# Increase tmux messages display duration from 750ms to 4s
set -g display-time 4000

# Refresh 'status-left' and 'status-right' more often, from every 15s to 5s
set -g status-interval 5

# Focus events enabled for terminals that support them
set -g focus-events on

# Super useful when using "grouped sessions" and multi-monitor setup
setw -g aggressive-resize on

# Yazi compatibility
set -g allow-passthrough on
set -ga update-environment TERM
set -ga update-environment TERM_PROGRAM

# Improve terminal colors
set -g default-terminal "screen-256color"
set -ga terminal-overrides ",xterm-256color:Tc"

# List of plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'vaaleyard/tmux-dotbar'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-continuum'
set -g @plugin 'yardnsm/tmux-1password'

set -g @plugin 'TanglingTreats/tmux-everforest' # Adds to the list of plugins
set -g @tmux-everforest 'dark-medium' # Sets the option to select the theme.

# Initialize TMUX plugin manager
run '$XDG_CONFIG_HOME/tmux/plugins/tpm/tpm'

# ── Post-theme overrides (must load after TPM to override everforest) ──────
# Dotbar at top
set -g @tmux-dotbar-position top

# Use pane titles as window names (Claude Code sets these with state spinners)
set -g automatic-rename on
set -g automatic-rename-format '#{=30:pane_title}'

# Start windows and panes numbering at 1 (instead of 0) and renumber when a window is closed
set-option -g renumber-windows on
set -g base-index 1
setw -g pane-base-index 1

# Claude state monitor: appends to status-right AFTER plugins finish
# The script re-applies window formats every refresh (plugins overwrite config-level overrides)
# Stoplight colors: green=#a7c080 idle, yellow=#dbbc7f working (everforest palette)
run-shell 'tmux set-option -ga status-right "#($HOME/.config/tmux/scripts/claude-pane-monitor.sh)"'
