# Remap prefix from 'C-b' to 'C-Space'
unbind C-b
set -g prefix C-Space

# [ PREFIX + r ] Reload tmux config
unbind r
bind r source-file $XDG_CONFIG_HOME/tmux/tmux.conf \; display "Reload Configuration"

# Mouse support
set -g mouse on

# Move between panes vim style
bind-key h select-pane -L
bind-key j select-pane -D
bind-key k select-pane -U
bind-key l select-pane -R

# Split panes using | and -
bind | split-window -h
bind - split-window -v
unbind '"'
unbind %

# Easier and faster switching between next/prev window
bind C-p previous-window
bind C-n next-window

# Toggle last window / last pane
bind Tab last-window
bind BTab last-pane

# TMUX Sensible settings but my tweaks
# Address vim mode switching delay (http://superuser.com/a/252717/65504)
set -s escape-time 0

# Increase scrollback buffer size from 2000 to 1000000 lines
set -g history-limit 1000000

# Increase tmux messages display duration from 750ms to 4s
set -g display-time 4000

# Refresh 'status-left' and 'status-right' more often, from every 15s to 5s
set -g status-interval 5

# Focus events enabled for terminals that support them
set -g focus-events on

# Super useful when using "grouped sessions" and multi-monitor setup
setw -g aggressive-resize on

# Yazi compatibility
set -g allow-passthrough on
set -ga update-environment TERM
set -ga update-environment TERM_PROGRAM

# Improve terminal colors
set -g default-terminal "screen-256color"
set -ga terminal-overrides ",xterm-256color:Tc"

# List of plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-continuum'
set -g @plugin 'yardnsm/tmux-1password'
set -g @plugin 'wfxr/tmux-power'
set -g @plugin 'jtbairdsr/tmux-inactive-panes'

# ── Theme: tmux-power w/ Everforest dark-hard ───────────────────────────
# Plugin options MUST be set before TPM init
set -g @tmux_power_theme 'everforest'

# Everforest dark-hard gradient (overrides tmux-power's neutral greys)
set -g @tmux_power_g0 '#1e2326'   # bg0  - status bar background
set -g @tmux_power_g1 '#272e33'   # bg1
set -g @tmux_power_g2 '#2e383c'   # bg2
set -g @tmux_power_g3 '#374247'   # bg3
set -g @tmux_power_g4 '#4f585e'   # grey1 - dim foreground

# Compact status bar (better with many windows)
set -g @tmux_power_show_upload_speed   false
set -g @tmux_power_show_download_speed false
set -g @tmux_power_show_web_reachable  false
set -g @tmux_power_prefix_highlight_pos 'R'

# ── Active pane indicator: tmux-inactive-panes ──────────────────────────
set -g @default-inactive-color 'dark'
set -g @active-bg-color-dark   '#272e33'   # bg1 - active pane "lifted"
set -g @active-fg-color-dark   '#a7c080'   # green - active border
set -g @inactive-bg-color-dark '#1e2326'   # bg0 - inactive matches terminal
set -g @inactive-fg-color-dark '#4f585e'   # grey1 - inactive border

# Initialize TMUX plugin manager
run '$XDG_CONFIG_HOME/tmux/plugins/tpm/tpm'

# ── Post-TPM overrides ────────────────────────────────────────────────────

# Use pane titles as window names (Claude Code sets these with state spinners)
set -g automatic-rename on
set -g automatic-rename-format '#{=30:pane_title}'

# Start windows and panes numbering at 1 and renumber on close
set -g renumber-windows on
set -g base-index 1
setw -g pane-base-index 1
