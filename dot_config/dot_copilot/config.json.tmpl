{{- /* GitHub Copilot CLI Configuration Template
Global settings for GitHub Copilot CLI.
OAuth tokens and user authentication are preserved separately.
*/ -}}
{
  "banner": "never",
  "model": "gpt-5.4",
  "render_markdown": true,
  "theme": "auto",
  "trusted_folders": [
    "{{ env "HOME" }}/.local/share/chezmoi"{{ if hasPrefix "personal" .machine }},
    "{{ env "HOME" }}/projects/whistlepost"{{ end }}{{ if hasPrefix "work" .machine }},
    "{{ env "HOME" }}/projects/iac"{{ end }}
  ],
  "last_logged_in_user": {
    "host": "https://github.com",
    "login": "stevencarpenter"
  },
  "logged_in_users": []
}
