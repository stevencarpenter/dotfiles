{{/* Lab-mac MCP overlay.

     Curated set for the homelab box (2019 i9 MacBook Pro, headless +
     Screen Share). Lab is a standalone home server, unrelated to
     personal-mac: the only lab-relevant server from the shared set is the
     local Grafana instance for home-ops (defined once in mcp-master.json,
     shared with personal — master.json also defines codegraph, which is
     shared with every machine, not lab-specific). Lab does not run hippo
     (no local LM brain), and hippo is not shared from personal. This
     overlay is intentionally empty — lab wants exactly the master-config
     defaults, no server here needs enabling or disabling.

     github MCP comes from the github@claude-plugins-official plugin (enabled
     in dot_claude/modify_settings.json.tmpl), not from a server defined here
     or in mcp-master.json. */}}
{
  "$schema": "https://modelcontextprotocol.io/schema/config.json",
  "servers": {}
}
