# Exercises

Vim-golf challenges moved to the month-agnostic `~/projects/vim-golf` repo
(challenges under `challenges/YYYY-MM-DD`, runner `./vim-golf`).
`scripts/vim-golf` delegates there; `just vim-golf` still works.
