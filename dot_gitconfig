[core]
    pager = delta
	autocrlf = input

[interactive]
    diffFilter = delta --color-only

[delta]
    navigate = true  # use n and N to move between diff sections
    dark = true      # or light = true, or omit for auto-detection

[merge]
    conflictStyle = zdiff3
[init]
	defaultBranch = main
[maintenance]
	strategy = incremental
	repo = /Users/scarpenter/projects/banking
  repo = /Users/scarpenter/projects/iac
  repo = /Users/scarpenter/projects/configuration
