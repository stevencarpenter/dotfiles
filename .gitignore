# macOS
.DS_Store
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes
.fseventsd

# Python (mcp_sync, aws_config_gen, token_auditor)
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
.coverage.*
htmlcov/
*.egg-info/
dist/
build/

# JetBrains IDEs (root .idea/, but keep tracked *.iml in subprojects)
/.idea/
*.iws

# VS Code
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json

# Claude project transient data
.claude/debug
.claude/downloads
.claude/file-history
.claude/history.jsonl
.claude/ide
.claude/plans
.claude/plugins
.claude/projects
.claude/session-env
.claude/shell-snapshots
.claude/stats-cache.json
.claude/statsig
.claude/telemetry
.claude/todos
.claude/settings.local.json

# Repo-local / machine-specific (defensive, files don't currently exist)
.envrc
private_dot_claude.json
*threat-model*.md

# All CLAUDE.md files except root and dot_claude/ (tracked) — explicit allowlist
**/CLAUDE.md
!/CLAUDE.md
!/dot_claude/CLAUDE.md

# Serena MCP cache
.serena/

# Codex sandbox env (machine-local)
/.codex/environments/environment.toml

# understand-anything: track the graph + fingerprints + meta, but not the
# transient per-run scratch dirs (recreated and deleted on each /understand run)
.understand-anything/intermediate/
.understand-anything/tmp/
