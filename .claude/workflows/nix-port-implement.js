export const meta = {
    name: 'nix-port-implement',
    description: 'Write the nix-darwin/home-manager tree: flake, modules, template ports, then retire chezmoi machinery',
    phases: [
        {title: 'Build', detail: 'T1-T6 parallel implementers'},
        {title: 'Finish', detail: 'T7 repo-infra retirement + docs + CI'},
    ],
}

const SCRATCH = '/private/tmp/claude-501/-Users-carpenter--local-share-chezmoi-full-nix-darwin-dotfile-shape/ffbb24ab-44ff-48ab-8508-a6694ec8e824/scratchpad'
const PRELUDE = 'You are one implementer in a coordinated port of a chezmoi dotfiles repo to nix-darwin + home-manager. FIRST read the locked contract at ' + SCRATCH + '/CONTRACT.md — it defines the layout, specialArgs interface, gating map, and YOUR task (paths you own). Obey the HARD RULES: no git commands ever (use plain rm for deletions), stay inside your assigned paths, never decrypt .age files, nix cannot be executed on this machine so write carefully and conservatively (prefer syntax you are certain of). Repo: /Users/carpenter/.local/share/chezmoi-full-nix-darwin-dotfile-shape. When done, return a concise report: files created/edited/deleted, decisions made, anything you could not verify.\n\n'

phase('Build')
const build = await parallel([
    () => agent(PRELUDE + 'Your task is T1 core-flake. Also read the architecture SME entry in ' + SCRATCH + '/phase1-results.json (smes[0]) for the mkHost sketch. Write flake.nix (one-screen mkHost fold over lib/machines.nix, agenix input included, home-manager wired with useGlobalPkgs/useUserPackages/extraSpecialArgs per contract), lib/machines.nix (with capability doc comments carried from .chezmoidata/machines.toml), hosts/personal-mac.nix, hosts/work-mac.nix, hosts/lab-mac.nix (thin: import the shared module sets; per-host secrets declarations will be added by T6 — leave a clearly marked section comment where host-scoped config goes), modules/darwin/default.nix (imports core, macos-defaults, homebrew), modules/home/default.nix (imports dotfiles, shell, packages, tiling, dev-tools, ai-stack, secrets, sync-hooks), bootstrap.sh (Determinate Nix install, op read of age key to ~/.config/chezmoi/key.txt, ln -sfn repo ~/.dotfiles, hostname->config map via scutil --get LocalHostName with a prompt fallback, first darwin-rebuild switch), rebuild.sh (relink + sudo darwin-rebuild switch --flake ~/.dotfiles#<detected-host>). Update .gitignore for nix artifacts (result, .direnv). The module files you import are written by other agents concurrently — just import them by the contract names; do NOT create placeholder module files (except default.nix themselves).', {
        label: 'T1-core-flake',
        phase: 'Build',
        model: 'opus',
        effort: 'medium'
    }),
    () => agent(PRELUDE + 'Your task is T2 darwin-system. Also read phase1-results.json domains entry "packages-macos-system" and smes entry on the package split, plus ' + SCRATCH + '/hooks-spec.md (bucket A specifics). Read dot_config/homebrew/Brewfile.tmpl and .chezmoiscripts/darwin/{run_onchange_configure-macos-defaults.sh,run_onchange_set-login-shell.sh,run_once_setup-macos.sh} and Library/... plist is already at home/Library/LaunchAgents/com.user.maxfiles.plist — read it. Write modules/darwin/core.nix (nix.enable=false, nixpkgs.config.allowUnfree, system.primaryUser, users.users, system.stateVersion, login shell pin + stale launchctl SHELL clearing activation, launchd port of the maxfiles plist — then rm home/Library after porting), modules/darwin/macos-defaults.nix (every defaults write mapped to system.defaults.* or CustomUserPreferences; screenshots dir + Spotlight sentinel files as a small activation; comment any mapping you are unsure of with the original defaults command), modules/darwin/homebrew.nix (nix-homebrew + homebrew module: taps/brews/casks with caps gating per the SME split; cleanup="none" with graduation comment; mactop aarch64-only), modules/home/packages.nix (core CLI + fonts, dev fonts gated caps.dev, conftest gated identity=="work", uv/python3/mise/age included, NO chezmoi). Then rm dot_config/homebrew/Brewfile.tmpl (and its dir if empty) and the three .chezmoiscripts/darwin scripts you ported. Write modules/darwin/BOOTSTRAP-NOTES.md with anything from run_once_setup-macos.sh that must live in bootstrap.sh/just bootstrap (T7 merges it).', {
        label: 'T2-darwin-system',
        phase: 'Build',
        model: 'opus',
        effort: 'medium'
    }),
    () => agent(PRELUDE + 'Your task is T3 home-shell. Also read phase1-results.json domains entry "shell + secrets" (the per-file gating table). Write modules/home/dotfiles.nix implementing the FULL gating map from the contract section "Gating map" using mkOutOfStoreSymlink through ~/.dotfiles for every raw file/dir under home/ (link whole directories where the dir is uniform, individual files where gating differs within a dir — profile.d needs per-file gating). Write modules/home/shell.nix (programs.zsh.enable=false with a comment why — z4h owns the shell; home.sessionVariables only if needed; environment shells handled in darwin core — do not duplicate). Replace the chezmoi-apply lib: write home/.config/zsh/lib/rebuild.zsh defining a rebuild() function (and a transitional ca() alias pointing at it, echoing a deprecation note) that detects the host via scutil --get LocalHostName mapped to flake config names (personal-mac/work-mac/lab-mac, with $DOTFILES_HOST override) and runs sudo darwin-rebuild switch --flake ~/.dotfiles#<host>; then edit home/.config/zsh/.zshrc: update the source line that currently sources lib/chezmoi-apply.zsh (and any ca-related aliases) to source lib/rebuild.zsh instead — verify by reading the .zshrc first; rm home/.config/zsh/lib/chezmoi-apply.zsh. Do not otherwise modify .zshrc content. IMPORTANT: your sandbox may block writing files named .zshrc — if an Edit to .zshrc fails with Operation not permitted, report it in your final message instead of retrying endlessly.', {
        label: 'T3-home-shell',
        phase: 'Build',
        model: 'opus',
        effort: 'medium'
    }),
    () => agent(PRELUDE + 'Your task is T4 tiling. Read ' + SCRATCH + '/tiling-spec.md for the exact template-diff analysis. Read dot_config/aerospace/aerospace.toml.tmpl and materialize TWO complete static files: home/.config/aerospace/aerospace.personal.toml (personal blocks resolved in) and home/.config/aerospace/aerospace.work.toml (work blocks + the three corporate floating rules) — shared content identical in both, byte-for-byte from the template minus Go-template syntax. Read dot_config/sketchybar/machine.env.tmpl. Write modules/home/tiling.nix: gated lib.mkIf caps.tiling — symlink ~/.config/aerospace/aerospace.toml to the identity-appropriate static file via mkOutOfStoreSymlink, symlink the layouts dir only when identity=="personal", symlink sketchybar dir contents (the raw sketchybarrc/items/plugins are at home/.config/sketchybar) EXCEPT machine.env which is nix-generated: home.file.".config/sketchybar/machine.env".text from caps.sketchybar_workspace_badges; symlink borders. Port the restart hook per tiling-spec.md into home.activation.startTilingStack (read .chezmoiscripts/darwin/run_onchange_after_start-tiling-stack.sh.tmpl for the body). CAREFUL: modules/home/dotfiles.nix (owned by T3) also covers gating — the contract assigns aerospace/sketchybar/borders symlinks to YOUR tiling.nix; T3 was told caps.tiling gating exists — coordinate by contract: tiling.nix owns ALL tiling-stack links, dotfiles.nix must not duplicate them (state this in a comment at the top of tiling.nix). Then rm the two .tmpl sources and the .chezmoiscripts tiling script.', {
        label: 'T4-tiling',
        phase: 'Build',
        model: 'sonnet',
        effort: 'high'
    }),
    () => agent(PRELUDE + 'Your task is T5 ai-stack + dev-tools + sync-hooks. Read phase1-results.json domains entries "ai-tooling" and "dev-tools", plus ' + SCRATCH + '/hooks-spec.md (bucket B sketches — follow them closely). Work items: (1) Read dot_claude/modify_settings.json.tmpl fully; produce home/.claude/settings-base.json (conditionals resolved OUT into capability-varying values) and write the merge logic as a home.activation entry in modules/home/ai-stack.nix that preserves the documented merge semantics (SessionStart hook union etc.) using jq, with capability-dependent values (plugin enable flags per caps.dev etc.) interpolated from nix; if the template logic is too conditional for one base file, generate the per-host JSON fragment from nix (home.file text via builtins.toJSON) and jq-merge that — your choice, document it. (2) Read dot_config/mcp/machine/{lab,personal}.json.tmpl; materialize static home/.config/mcp/machine/{lab.json,personal.json}. (3) Read dot_config/agent-journal/*.tmpl; materialize static home/.config/agent-journal/{config.toml,workstreams.toml} (personal-only). (4) Read dot_config/mise/config.toml.tmpl; produce home/.config/mise/config.toml (ungated base) + home/.config/mise/conf.d/dev.toml + home/.config/mise/conf.d/infra.toml (the gated blocks); verify mise supports conf.d merging via its docs knowledge — if uncertain, note it and ALSO provide the fallback (three per-host full files) in a comment. (5) Rename dot_config/git/config.tmpl content to home/.config/git/config (it has zero template directives — read to confirm, strip nothing). (6) Write modules/home/dev-tools.nix: mise config symlinks (base always, conf.d/dev.toml when caps.dev, conf.d/infra.toml when caps.infra). (7) Write modules/home/ai-stack.nix: settings merge activation + gated symlinks ONLY for pieces the contract does not assign to dotfiles.nix (mcp machine overlay by identity is assigned to T3 dotfiles.nix — put a boundary comment; ai-stack.nix owns: settings-base merge, ~/.claude/skills interplay notes). (8) Write modules/home/sync-hooks.nix per hooks-spec.md bucket B exactly (mcpSync, skillsSync with --repo-root, awsConfigGen, agentsInstall; UV_PYTHON from pkgs; warn-never-fail). Then rm every .tmpl you ported: dot_claude/modify_settings.json.tmpl, dot_config/mcp/machine/*.tmpl, dot_config/agent-journal/*.tmpl, dot_config/mise/config.toml.tmpl, dot_config/git/config.tmpl.', {
        label: 'T5-ai-stack',
        phase: 'Build',
        model: 'opus',
        effort: 'medium'
    }),
    () => agent(PRELUDE + 'Your task is T6 secrets. Read the secrets SME entry in phase1-results.json (smes[1]) — follow its agenix design. Inventory the actual blobs with: ls -R secrets/ (30 files). Write modules/home/secrets.nix: imports inputs.agenix.homeManagerModules.default; age.identityPaths = existing chezmoi key path; age.secrets entries — common zsh-env on all hosts; ssh-config on identity != "work"; personal env on identity=="personal"; work env + aws overrides on identity=="work"; the 25 claude-skills blobs via a fold over builtins.readDir (or an explicit list if readDir over the secrets dir is awkward — explicit is fine and more readable) landing at ~/.claude/skills/<skill>/<relpath>, scripts mode 0700 (the .sh/.py under scripts/), others 0600, all gated identity=="work" AND caps.skills. Add a module comment explaining ordering vs the skillsSync activation (agenix activation must precede it; verify what DAG stage agenix home-manager module uses and note it). Write secrets/secrets.nix (recipients file for future agenix -e; document the existing recipient age1462h0ed4ufkjrq0wu326l30c8hay9uewlsaudk89mgqjc5540vrqacejsz). Write secrets/README.md: layout, the bootstrap key flow (op read into ~/.config/chezmoi/key.txt before first switch), how to add/rotate a secret with agenix -e, and the deferred one-time migration steps (optional re-encrypt to agenix-native recipients; optional 1Password opnix end-state per the repo 2026-07-02 plan doc). Do NOT decrypt anything.', {
        label: 'T6-secrets',
        phase: 'Build',
        model: 'sonnet',
        effort: 'high'
    }),
])

log('Build stage done: ' + build.filter(Boolean).length + '/6 reports')

phase('Finish')
const t7 = await agent(PRELUDE + 'Your task is T7 repo-infra (the finisher — T1-T6 already ran; their reports follow at the end). Read phase1-results.json domains entry "repo-infrastructure". Execute the full T7 assignment from the contract: delete ALL remaining chezmoi machinery files (.chezmoi.toml.tmpl, .chezmoiencrypt.toml, .chezmoiexternal.toml.tmpl, .chezmoiignore, .chezmoidata/ — but FIRST carry the token-auditor version pin from .chezmoidata/tools.toml into the Justfile sync recipe —, .chezmoiscripts/ remnants, .chezmoitemplates/, and any leftover dot_* / private_* source dirs — verify each is truly ported first by listing what remains and cross-checking the T1-T6 reports; if something is NOT ported, leave it and flag it). Merge modules/darwin/BOOTSTRAP-NOTES.md content into bootstrap.sh (T1 wrote it) as a "just bootstrap"-adjacent section or Justfile bootstrap recipe, then rm the notes file. Rewrite Justfile per contract (keep python lint/test/fmt recipes verbatim; add: rebuild, check (nix flake check --no-build), sync (agent-registry clone/refresh + token-auditor pinned install), bootstrap; drop chezmoi recipes; audit-capabilities: point it at lib/machines.nix or drop with a note — your call, document it). Rewrite README.md for the nix shape (what it is, 3 hosts + capability table pointer, bootstrap/rebuild flow, secrets flow summary, repo tour, thin-wrapper explanation — model it on kunchenguid/dotfiles README structure but for this multi-host repo). Update CLAUDE.md: replace chezmoi-specific sections (naming conventions, chezmoi commands, machine-gating description) with the nix equivalents (layout, machines.nix capability table, rebuild/check commands, module conventions); KEEP the python tools sections, commit/PR rules, IntelliJ MCP note. Update .github/workflows/dotfiles-hygiene-ci.yml: remove chezmoi install/execute-template steps, add a nix flake check job (runs-on macos-latest, uses DeterminateSystems/nix-installer-action@main then nix flake check --no-build and nix fmt -- --check if a formatter is configured — do not add a formatter), keep still-relevant script checks. Review scripts/*.sh: rm the ones testing chezmoi mechanics (test-ca-*.sh); keep and if needed path-fix the still-live ones (statusline, sketchybar, mcp-sync-ci, claude-settings-order — they reference dot_config/ paths that moved to home/.config/: update paths). Update .pre-commit-config.yaml (machine-capability-audit hook: retarget or drop consistent with your Justfile decision). Write docs/nix-migration.md: complete mapping table chezmoi mechanism → nix mechanism (source the contract + your observations), including the deferred items (re-encryption, first-switch cautions like homebrew cleanup=none, nix not yet installed on any machine). Check .claude/skills/ repo-level skills for chezmoi-specific ones and add a one-line deprecation note at the top of each SKILL.md that no longer applies (do not delete them). T1-T6 REPORTS:\n\n' + build.filter(Boolean).map((r, i) => '--- T' + (i + 1) + ' ---\n' + (typeof r === 'string' ? r : JSON.stringify(r))).join('\n\n'), {
    label: 'T7-repo-infra',
    phase: 'Finish',
    model: 'opus',
    effort: 'medium'
})

return {buildReports: build, finishReport: t7}
