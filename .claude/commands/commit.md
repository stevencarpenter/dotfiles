Inspect the working tree and staged/unstaged changes. Commit the requested work on
an appropriate feature branch using the repository's workflow and checks. Preserve
unrelated changes and inspect untracked files before staging them.

Use a concise Conventional Commit message describing the actual change, without
attribution trailers. Report the commit and validation. A commit request alone does
not authorize pushing, opening a PR, or merging.
